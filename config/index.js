const reservedWords = require('./reserved-words');
const errors = require('./parsed-errors');
const types = require('./data-types');
const validateProperties = require('./validateProperties');

module.exports = {
  reservedWords,
  errors,
  types,
  validateProperties
};
