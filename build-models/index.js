const propertyGenerator = require('./property-generator');
const schemaDefinition = require('./schema-definition');
const autoGeneration = require('./auto-generation');

module.exports = {
  propertyGenerator,
  schemaDefinition,
  autoGeneration
};
